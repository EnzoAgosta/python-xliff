[project]
name = "python-xliff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Enzo Agosta" }]
requires-python = ">=3.13"
dependencies = [
    "lxml>=5.3.1",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
docs = []
dev = [
    "mypy>=1.15.0",
    "types-lxml>=2025.3.4",
]
